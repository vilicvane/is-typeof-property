{
  "name": "is-tuple",
  "version": "0.0.0",
  "description": "Simple utilities for tuple type narrowing...",
  "repository": "https://github.com/vilic/is-tuple.git",
  "license": "MIT",
  "author": "vilicvane",
  "main": "bld/library/index.js",
  "types": "bld/library/index.d.ts",
  "scripts": {
    "build": "rimraf ./bld && tsc --build",
    "lint": "run-in-every eslint-project --parallel --echo -- eslint --config {configFileName} .",
    "lint-prettier": "prettier --check .",
    "test": "yarn lint-prettier && yarn build && yarn lint"
  },
  "dependencies": {
    "tslib": "^2.3.1"
  },
  "devDependencies": {
    "@mufan/code": "^0.2.10",
    "@mufan/eslint-plugin": "^0.1.55",
    "eslint": "^8.12.0",
    "prettier": "^2.6.2",
    "rimraf": "^3.0.2",
    "run-in-every": "^0.2.0",
    "typescript": "^4.6.3"
  }
}
